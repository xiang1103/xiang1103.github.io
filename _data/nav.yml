# Sidebar nav.
#
# On-page section links are generated from _sections/*.md (any section with a
# `nav:` label) — do not list them here, or they'll appear twice.
#
# This file is only for links that are NOT sections: external destinations,
# other pages, downloads.
extra:
  - label: Resume
    href: "assets/files/Xiang Liu_Resume (4).pdf"
    icon: file
    external: true
