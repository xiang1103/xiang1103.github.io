---
layout: homepage
---

<!--
  The page is assembled from _sections/*.md, in `order` order, plus the hero
  fields in _config.yml. See CLAUDE.md §6.3 for the section contract.

  Anything written in this file renders in a free-form slot between the hero and
  the first section. It is markdown, and markdown may contain raw HTML. Leave it
  empty unless you want something there.
-->
