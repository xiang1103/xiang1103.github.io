source "https://rubygems.org"

# GitHub Pages builds the deployed site with its own pinned toolchain and ignores
# this file. It exists only so the site can be previewed locally.
gem "jekyll", "~> 3.8.5"
gem "webrick", "~> 1.8"

# Formerly part of the Ruby standard library; must be declared explicitly on
# Ruby 3.4+ for Jekyll 3.x to boot.
gem "csv"
gem "base64"
gem "logger"
gem "bigdecimal"
gem "rexml"
